import LoginForm from '@/app/ui/login-form';

const LoginPage = () => {
    return(
        <div className="container is-fluid">
            <LoginForm />
        </div>
    );
}

export default LoginPage;