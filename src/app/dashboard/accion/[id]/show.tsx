"use client";

import React, { useState } from "react";
import { User } from "@/app/utils/entities";

interface Props {
    user: User,
    seccionMenuId: number,
    seccionMenu: string,
    record: any,
}

export default function Show({ user, seccionMenuId, seccionMenu, record}: Props) {
    return (
        <>
      
        </>
    );
}